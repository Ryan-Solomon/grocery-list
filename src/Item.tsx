import React, { FC } from 'react';

// WORK ACTUALLY ADDING THE ITEM

type Props = {
  key: number;
  item: string;
};

const Item: FC<Props> = (props) => {
  return <div></div>;
};

export default Item;
